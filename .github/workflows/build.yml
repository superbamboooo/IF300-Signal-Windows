name: Build Windows EXE

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.10'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas numpy matplotlib akshare pyinstaller

    - name: Build Windows EXE
      run: |
        pyinstaller --noconfirm --onefile --windowed --name "MultiStrategy" --add-data "data_updater.py;." --add-data "weekend_data_updater.py;." --add-data "strategy_if300.py;." --add-data "strategy_weekend.py;." signal_app_main.py

    - name: Upload artifact
      uses: actions/upload-artifact@v4
      with:
        name: IF300-Signal-Windows
        path: dist/MultiStrategy.exe
        retention-days: 30
