# 🪟 Windows 版本使用说明 - 数据更新修复

## 🎯 问题和修复

### 问题
Windows EXE 版本点击"更新数据"时失败，无法获取最新行情数据。

### 原因
1. ❌ 构建配置（build.yml）没有打包 `data` 文件夹（包含历史CSV数据）
2. ❌ 使用 `--onefile` 模式导致路径查找复杂
3. ❌ 数据目录路径逻辑在打包后失效

### 修复
✅ 已全部修复：
- 改进 `get_data_path()` 函数，支持多种部署方式
- 更新 GitHub Actions 配置，正确打包 data 文件夹
- 改为 `--onedir` 模式，确保文件结构清晰
- 添加详细的日志和错误提示

---

## 📦 新的Windows版本结构

### 下载和解压后的文件布局

```
MultiStrategy/
├── MultiStrategy.exe          ← 主程序（双击运行）
├── _internal/                 ← Python库和依赖
│   ├── pandas/
│   ├── matplotlib/
│   └── ... (其他库)
└── data/                       ← ✅ 数据文件夹（新增）
    ├── IF_主连_季月合约连接_day.csv
    └── 159915_创业板ETF_day.csv
```

---

## 🚀 使用方法

### 方法1：直接运行（推荐）

```
1. 从 GitHub Actions 下载最新版本
2. 解压到任意文件夹
3. 双击 MultiStrategy.exe 运行
4. 点击"更新数据"即可正常更新 ✅
```

**关键要求**：
- ⚠️ **不要删除 `data/` 文件夹**
- ⚠️ **不要移动 `.exe` 文件到其他位置**（会找不到data文件夹）
- ✅ 可以重命名整个 `MultiStrategy/` 文件夹

---

### 方法2：使用ZIP版本

如果下载了 `MultiStrategy-Windows.zip`：

```
1. 解压 MultiStrategy-Windows.zip
2. 进入解压后的 MultiStrategy 文件夹
3. 双击 MultiStrategy.exe 运行
4. 正常使用
```

---

## ✅ 验证修复

启动应用后，查看以下位置的日志信息：

### Windows 命令行输出（如果以命令行运行）

```
[数据路径] 找到: C:\Users\...\MultiStrategy\data
```

说明数据目录找到了，更新应该正常工作。

### GUI 中的验证

1. 启动应用
2. 点击"更新数据"
3. 如果成功，会看到：
   ```
   ✓ 数据更新成功
   合约: IF2603
   最新价: 4731.2
   获取时间: 14:45:17
   ```

4. 如果仍有问题，会显示详细错误提示

---

## 🔧 故障排查

### 问题1：点击更新后没有反应

**可能原因**：
- 非交易日（周末/假日）
- 不在交易时段（15:00后）
- 网络连接问题

**解决**：
- 检查当前日期和时间是否在交易时段内
- 检查网络连接
- 查看弹出的错误提示信息

---

### 问题2：提示"数据文件不存在"

**可能原因**：
- `data/` 文件夹被删除了
- 文件夹被意外移动了位置
- 权限问题

**解决**：
1. 检查 `MultiStrategy.exe` 同级目录是否有 `data/` 文件夹
2. 如果没有，重新下载完整版本
3. 如果有但仍然出错，检查文件夹权限

---

### 问题3：提示"权限被拒绝"或"无法写入"

**可能原因**：
- data 文件夹在受保护的位置（如 C:\Program Files）
- 用户无写入权限

**解决**：
1. 将整个 `MultiStrategy/` 文件夹移到用户目录（如 Desktop）
2. 以管理员身份运行（右键→以管理员身份运行）
3. 检查文件夹属性→权限

---

### 问题4：更新后K线图仍无新数据

**可能原因**：
- 历史数据缺失（data/*.csv 为空）
- API 数据源失败

**解决**：
1. 检查 `data/` 文件夹中的 CSV 文件大小
   - 应该有几MB以上的数据
   - 如果为0字节，说明初始数据缺失

2. 下载完整的初始数据：
   - 如果使用的是旧版本，可能缺少完整的历史数据
   - 重新下载最新版本应该包含完整数据

---

## 📊 日志和诊断

### 查看详细日志

如果想看更详细的调试信息，可以从命令行运行：

```bash
cd MultiStrategy
MultiStrategy.exe
```

这样会显示所有日志信息，包括：
- 数据路径查找过程
- API 调用状态
- 数据验证结果

---

## 🔄 更新流程

### 自动更新

当新版本发布时：
1. GitHub Actions 自动构建
2. 新的 `MultiStrategy/` 文件夹上传到 Artifacts
3. 下载新版本，替换老版本即可
4. **data/ 文件夹会自动包含最新的历史数据**

---

## ⚙️ 高级配置

### 自定义数据文件夹位置

如果想使用单独的数据文件夹（便于数据备份），可以：

1. 在 `MultiStrategy.exe` 同级目录创建 `data/` 文件夹
2. 将 CSV 文件放入该文件夹
3. 运行应用

系统会自动检测并使用该目录。

### 多个实例使用相同数据

如果想在同一台电脑上运行多个 MultiStrategy 实例但共享数据：

```
c:\trading\
├── MultiStrategy-v1/
│   ├── MultiStrategy.exe
│   └── data/ → (链接到共享的data目录)
│
├── MultiStrategy-v2/
│   ├── MultiStrategy.exe
│   └── data/ → (链接到共享的data目录)
│
└── shared-data/
    ├── IF_主连_季月合约连接_day.csv
    └── 159915_创业板ETF_day.csv
```

---

## 📞 遇到问题

如果问题仍未解决：

1. **检查文件结构**
   ```
   MultiStrategy/
   ├── MultiStrategy.exe
   ├── _internal/
   └── data/  ← 这个必须存在
   ```

2. **查看错误信息**
   - 应用会显示明确的错误提示
   - 提示会包含：数据路径、原因、建议

3. **检查网络连接**
   - 打开浏览器访问金融网站，确保网络正常
   - 一些数据源需要能访问外网

4. **使用测试脚本**
   ```bash
   python test_data_sources.py
   ```
   测试各个数据源是否可用

---

## 🎉 总结

修复后的 Windows 版本现在能够：

✅ 自动检测和创建 data 目录
✅ 支持多种部署方式
✅ 提供详细的错误提示
✅ 正常的数据更新功能
✅ 包含完整的历史数据

**新版本会更稳定可靠！**

---

## 📝 版本信息

| 项目 | 信息 |
|------|------|
| **修复日期** | 2026-02-09 |
| **修复版本** | v1.0 |
| **构建方式** | GitHub Actions 自动构建 |
| **Python版本** | 3.10 |
| **打包方式** | PyInstaller --onedir |

---

## 参考文档

- [Windows 构建指南](WINDOWS_BUILD_GUIDE.md)
- [使用说明](FINAL_USAGE.md)
- [快速参考](QUICK_REFERENCE.txt)
